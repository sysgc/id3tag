# secrets / local config — copy .env.example instead
.env

# this app's persistent data (scan cache) when using the default ./data
# bind mount path from .env.example
/data/

# generated at Docker build time by pdoc — see app/Dockerfile
app/docs/

# Python
__pycache__/
*.pyc

# OS
.DS_Store

# local-only docs / logs
deployment.md
CLAUDE.md
prompts.md

# local Claude Code permission overrides
.claude/settings.local.json
